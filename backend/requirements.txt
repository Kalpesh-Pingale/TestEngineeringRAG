fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
aiohttp>=3.10.0
numpy>=2.0.0
python-dotenv>=1.0.1
# Local embeddings via ONNX runtime — no PyTorch, no model server, no API key.
fastembed>=0.4.0
